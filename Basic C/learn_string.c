#include<stdio.h>
int main() {

    char a;
    char arr[10];

    ///use gets to take with spaces
    ///gets(arr);
    scanf("%s", &arr);
    printf("%s", arr);

    ///Bangladesh India Different Country

    return 0;
}
