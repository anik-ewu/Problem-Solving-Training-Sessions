#include<stdio.h>

int main() {

    int a, b, res;
    scanf("%d%d", &a, &b);

    res = (a * a) + (2 * a * b) + (b * b);

    printf("First Solution : %d", res);


    (x * x) + ((a - b) * x ) - (a * b)

    return 0;
}
